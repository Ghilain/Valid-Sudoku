/**
 * @param {character[][]} board
 * @return {boolean}
 */
var isValidSudoku = function(board) {
    // decrale variables 
    const found = new Set();
    let block;
    // decrale loop used for row
    for (let i=0; i<9; ++i) {
        // decrale loop for columns
        for (let j=0; j<9; ++j) {

        }
    }
};